{
  "name": "helpdesk-ticket-system",
  "version": "1.0.0",
  "scripts": {
    "install": "npm --prefix backend install --legacy-peer-deps && npm --prefix frontend install --legacy-peer-deps",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm --prefix frontend build",
    "start": "npm --prefix backend start",
    "migrate:dev": "npm --prefix backend run migrate:dev",
    "seed:dev": "npm --prefix backend run seed:dev"
  }
}
